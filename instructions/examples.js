// Инструкция — выполняет действие:
let number = 10; // Инструкция присваивания значения переменной

// Выражение — возвращает значение:
5 + 15 // Выражение, которое возвращает 20

// Инструкция-выражение — выполняет действие и возвращает значение:
let result = 5 + 15; // Инструкция-выражение: присваивает результат выражения 5 + 15 переменной result

let a;

const b = 5;

if (a < b) {
    console.log('a is larger');
}

for (let i = 0; i <= 5; i++) {
    console.log(i);
}